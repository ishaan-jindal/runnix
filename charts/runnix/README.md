# Runnix Helm chart (deferred: helm placeholder)

Deferred: RuntimeClass kata-gvisor, NATS cluster, Postgres, gateway/dispatcher
Deployments + HPA, ServiceMonitors. Docker-first scaffold uses deploy/compose.yaml.

# Runner images (Phase 1+ placeholder)

Shared sandbox base + per-language images (python/c/java) with
RuntimeClass kata-gvisor, cap-drop=ALL, read-only rootfs, 128m/0.5CPU/32pids.
See vault Architecture for the Job spec contract.
